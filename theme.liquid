<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ page_title }}</title>
    <!-- Add your CSS and JavaScript links here -->
    {{ content_for_header }}
</head>
<body>
    {{ content_for_layout }}
    {{ 'theme.js' | asset_url | script_tag }}
</body>
</html>
